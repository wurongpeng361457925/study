http {
	server {
		listen	80;
		server_name		www.domain1.com;
		access_log		logs/doman1.access.log main;
		location/ {
			index index.html;
			root /var/www/domain1.com/htdocs;
		}
	}
	server {
		listen	80;
		server_name		www.domain2.com;
		access_log		logs/domaim2.access.log main;
		location/ {
			index index.html;
			root /var/www/domian2.com/htdocs;
		}
	}
}
全局块配置
	---配置运行nginx服务器用户(组)
		worker process数
		Nginx进程pid存放路径
		错误日志存放路径
		配置文件的引入
events {
	此配置主要影响nginx服务器与用户的网络连接
		设置网络连接的序列化
		是否允许同时接收多个网络连接
		事件驱动模型的选择
		最大连接数的配置
}
http {	
	定义MIME-tYPE
	自定义服务日志
	允许sendfile方式传输文件
	连接超时时间
	单连接请求数上限

	server1 {
		server全局块	
			配置网络监听
			基于名称的虚拟主机配置
				(虚拟主机：一种特殊的软硬件技术。可以将网络上的每一台计算机分成多个
				 虚拟主机，每个虚拟主机可以独立对外提供www服务，这样就可以实现一台主机对外提供多个
				 web服务，每个虚拟主机之间是独立，互不影响)
			基于IP的虚拟主机配置
		
		location1 {
			请求根目录配置
			更改location的uri
			网站默认首页配置
		}
		location2 {}
		...
	}
	server2 {
		server全局块
		location1 {}
		location2 {}
		...
	}
}