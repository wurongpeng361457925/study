idea快捷键   
	
	---自动补全返回值---

		new ArrayList<String>(); 自动补全 ArrayList<String> =

		直接输入  new ArrayList<String>().var
  
		再按tab键即可 补全 ArrayList<String> = 

	---循环的快捷键---
		
		输入 itar ，按tab
		生成普通for循环
		for (int i = 0; i < array.length; i++) {
            		 = array[i];
            
       		 }

		
		输入 itco, 按tab
		生成Collection迭代
		for (Iterator<String> iterator = locationUrl.iterator(); iterator.hasNext(); ) {
          	  String next =  iterator.next();
            
       		}

		输入 iten 
		生成enumeration遍历
		while (enumeration.hasMoreElements()) {
           		 Object nextElement =  enumeration.nextElement();
            
        	}

		输入iter
		生成增强foreach循环
		for (String s : locationUrl) {
            
        	}

		输入itit
		生成iterator迭代
		while (iterator.hasNext()) {
               	 	Object next =  iterator.next();
                }

	---生成getter,setter,toString()，新建Java类 等等---
		
		在类中 按 alt+insert  生成getter，setter，tostirng()等
		选择一个文件夹， 按 alt+insert 新建一个java类

	---格式化代码，代码对齐---
		
		ctrl + alt + L

	---代码窗口最大化---
		
		ctrl + shift + F12